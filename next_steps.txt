- this branch will close all 3 PRs - done
- need to configure github actions so that pre-commit hooks run & then the inbuilt tests run - done boss
- deploy app directly to railway without db setup
- backup db to railway
- set up gh action to auto deploy master (test)
- check if they support staging


- then setup railway deployment
    - checkout whether they support staging deployment


- backup whole db
    - create a good-first-issue on pylinting the whole code
    - heroku backup db to railway
